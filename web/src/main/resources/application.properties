server.port=8081
spring.thymeleaf.cache=false

# obs-experiment-coroot branch: Sentry config removed (Phase 2).
# Coroot's eBPF node-agent observes this JVM at the kernel level — no
# in-process SDK or env config is required. The portfolio orchestrator
# may still set SENTRY_* env vars at the container level for the master
# branch's benefit; on this branch they're simply ignored.
